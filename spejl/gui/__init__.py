"""The desktop app — Screen 1/2 of the build plan's §06 UI flow.

A thin client of the same ``mirror_pdf`` / ``mirror_raster`` the CLI
calls; the app never re-implements pipeline logic, only presents it.
"""
